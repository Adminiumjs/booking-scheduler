/* PORT-STUB: waitlist — replace with the ported screen. */

import "../styles/screen-waitlist.css";

export default function Waitlist() {
  return (
    <section className="bk-page scr-waitlist">
      <h1>Join the waitlist</h1>
      <p>Not ported yet.</p>
    </section>
  );
}
