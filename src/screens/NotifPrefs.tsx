/* PORT-STUB: notifprefs — replace with the ported screen. */

import "../styles/screen-notifprefs.css";

export default function NotifPrefs() {
  return (
    <section className="bk-page scr-notifprefs">
      <h1>Notifications</h1>
      <p>Not ported yet.</p>
    </section>
  );
}
